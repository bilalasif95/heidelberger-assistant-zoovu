import globals from "../partials/global-variables";
import stylesConfig from "../styles.config";

export default {
    container: {
        '&:focus': {
            outline: 'none',
        },
        '& .search-wrapper': {
            position: 'relative',
        },
    }
};
